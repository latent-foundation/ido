---
title: Configure VLAN segmentation on the switch
status: planning
priority: normal
tags: homelab, networking
order: 0
---

Waiting until the firewall rules between segments are actually written before flipping this on —
no point isolating the IoT devices if the rule set between VLANs is wide open by default.

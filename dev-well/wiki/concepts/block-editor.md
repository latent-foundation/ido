# block-editor

The live-preview surface. The source is segmented into top-level blocks; the block
under the cursor swaps to a textarea while every other block stays rendered.

This page is the `backlinks` fixture — exactly one page links here, [[well]], so a
backlinks call against `block-editor` has a small, predictable answer.

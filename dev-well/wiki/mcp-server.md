# mcp-server

The read-only MCP server over one well: seven tools, stdio transport, no network.
One well per process — see [[well]] for what it's serving.

Search is keyword-only until the semantic index lands, so a query should use the
literal words the writing would use rather than a paraphrase.

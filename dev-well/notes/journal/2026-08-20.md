# 2026-08-20

Poked at the semantic index plan again. The embedding model choice is still open —
candle with a small sentence-transformer is the leading option, mostly because it
keeps the whole thing on-device and needs no network at query time.

Related: [[mcp-server]].

- [x] read the rmcp changelog
- [ ] sketch the RRF fusion step
- [ ] decide where the index lives under `.ido/`

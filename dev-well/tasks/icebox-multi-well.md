---
title: Multi-well support in the MCP server
status: icebox
priority: low
tags: mcp
order: 0
---

A `well` parameter on every tool plus a `list_wells` tool. Deliberately deferred.

The status here (`icebox`) is not one of the board's columns, so this task is an
orphan: it shows in the backlog but is never renumbered or targeted by a drop.

See [[mcp-server]].
